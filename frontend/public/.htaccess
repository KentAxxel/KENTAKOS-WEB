<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /kentakitos/
  
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /kentakitos/index.html [L]
</IfModule>
